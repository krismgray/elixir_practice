defmodule Golf do
  def sum_array do
    t = 0
    a.each { |i| total += i.to_i }
    [a,t]
  end
end

translate to elixir. Create a method that takes in an array sums the array then returns the array and the sum.

seconds golf hole.
HOLE 2: Rock paper scissors
Takes a user input the computer randomly chooses and the winner is output to the screen.
